# 化妆品官网与后台管理系统需求文档 (2/8)

## 🎯 前台官网功能模块详细说明

### 2. 产品中心功能设计

#### 2.1 产品列表页

**筛选器配置**：

```typescript
interface FilterConfig {
  priceRange: {
    min: number;
    max: number;
    step: number;
  };
  efficacy: Array<{ label: string; value: string; count: number }>;
  skinType: Array<{ label: string; value: string }>;
  series: Array<{ label: string; value: string }>;
}
```

#### 2.2 产品详情页核心功能

**规格选择器**：

```typescript
interface ProductSpec {
  specId: string;
  specName: string;        // 颜色、容量
  specValues: Array<{
    valueId: string;
    valueName: string;     // 自然色、30ml
    stock: number;
    price: number;
    image: string;
    isAvailable: boolean;
  }>;
}
```

**购买操作流程**：

```
用户选择规格 → 验证库存 → 点击按钮
                              ↓
                    [立即购买] or [加入购物车]
                              ↓
              [立即购买]                [加入购物车]
                  ↓                          ↓
            跳转结算页              显示成功提示动画
                                          ↓
                                    购物车数量+1
```

---

### 3. 购物车与结算

#### 3.1 购物车功能

**购物车数据结构**：

```typescript
interface CartItem {
  cartId: string;
  productId: string;
  productName: string;
  productImage: string;
  specInfo: string;          // "自然色 / 30ml"
  price: number;
  quantity: number;
  stock: number;
  isSelected: boolean;       // 是否选中
  isValid: boolean;          // 是否有效（库存充足、未下架）
  invalidReason?: string;    // 失效原因
}

interface Cart {
  items: CartItem[];
  selectedCount: number;     // 已选商品数量
  totalPrice: number;        // 已选商品总价
  discountAmount: number;    // 优惠金额
  finalPrice: number;        // 最终价格
}
```

**购物车操作**：

| 操作 | 功能说明 | 实现细节 |
|------|---------|---------|
| **全选/反选** | 批量选择商品 | 更新所有商品的isSelected状态 |
| **修改数量** | +/- 按钮或输入框 | 实时验证库存，超出库存提示 |
| **删除商品** | 单个删除或批量删除 | 二次确认弹窗 |
| **移入收藏** | 删除的同时加入收藏夹 | 需登录 |
| **清空失效** | 一键清除失效商品 | 批量删除 |
| **实时计算** | 价格自动更新 | 防抖处理，减少计算频率 |

#### 3.2 结算页面

**页面结构**：

```
+--------------------------------------------------+
|  收货地址                                         |
|  ○ 张三 138****5678                              |
|    北京市朝阳区xxx街道xxx号                        |
|  ○ 李四 139****1234                              |
|    上海市浦东新区xxx路xxx号                        |
|  [+ 新增收货地址]                                 |
+--------------------------------------------------+
|  配送方式                                         |
|  ○ 顺丰快递 (预计2-3天送达) 运费: ¥10            |
|  ○ 韵达快递 (预计3-5天送达) 运费: ¥0 (满99免邮) |
+--------------------------------------------------+
|  发票信息                                         |
|  ○ 不开发票                                       |
|  ○ 个人发票  [填写发票信息]                       |
|  ○ 企业发票  [填写企业信息]                       |
+--------------------------------------------------+
|  优惠券                                           |
|  [选择优惠券] 当前可用3张                         |
|  已选: 满300减50                                  |
+--------------------------------------------------+
|  商品清单                                         |
|  [商品1图片] 商品名称 × 2     ¥299 × 2 = ¥598   |
|  [商品2图片] 商品名称 × 1     ¥399 × 1 = ¥399   |
+--------------------------------------------------+
|  订单备注                                         |
|  [文本输入框]                                     |
+--------------------------------------------------+
|  价格明细                         商品总额: ¥997 |
|                                    运费: ¥10    |
|                                  优惠券: -¥50   |
|                                  ---------------  |
|                        [提交订单]  实付: ¥957   |
+--------------------------------------------------+
```

**地址选择器**：

```typescript
interface Address {
  addressId: string;
  name: string;
  phone: string;
  province: string;
  city: string;
  district: string;
  detail: string;
  postalCode: string;
  isDefault: boolean;
  tag?: string;              // 家、公司
}
```

#### 3.3 支付页面

**支付方式**：

```typescript
interface PaymentMethod {
  method: 'wechat' | 'alipay' | 'balance';
  name: string;
  icon: string;
  isAvailable: boolean;
  tips?: string;
}

const paymentMethods = [
  {
    method: 'wechat',
    name: '微信支付',
    icon: '/icons/wechat-pay.svg',
    isAvailable: true
  },
  {
    method: 'alipay',
    name: '支付宝',
    icon: '/icons/alipay.svg',
    isAvailable: true
  },
  {
    method: 'balance',
    name: '余额支付',
    icon: '/icons/balance.svg',
    isAvailable: true,
    tips: '当前余额: ¥1,234.56'
  }
]
```

**支付流程**：

```
[确认订单] → [生成订单号] → [选择支付方式]
                                  ↓
                    [微信]  [支付宝]  [余额]
                      ↓        ↓        ↓
                   扫码支付  跳转支付  密码验证
                      ↓        ↓        ↓
                    [支付成功] ← 回调验证
                          ↓
                    [订单详情页]
```

---

### 4. 用户中心

#### 4.1 个人信息管理

**用户信息表单**：

```typescript
interface UserProfile {
  avatar: string;
  nickname: string;
  realName?: string;
  gender: 'male' | 'female' | 'unknown';
  birthday?: Date;
  phone: string;
  email?: string;
  idCard?: string;           // 身份证（脱敏）

  // 会员信息
  memberLevel: string;
  points: number;
  growth: number;

  // 账号绑定
  bindings: {
    wechat?: string;
    qq?: string;
    weibo?: string;
  };
}
```

#### 4.2 订单管理

**订单状态流转**：

```
待付款 → 待发货 → 待收货 → 已完成
  ↓                          ↓
取消订单                   评价/售后
```

**订单操作权限**：

| 订单状态 | 可执行操作 |
|---------|----------|
| **待付款** | 去支付、取消订单、修改地址 |
| **待发货** | 提醒发货、申请退款 |
| **待收货** | 查看物流、延长收货、申请退货 |
| **已完成** | 评价商品、申请售后、再次购买 |
| **已取消** | 删除订单、再次购买 |

#### 4.3 收藏夹

**收藏类型**：

```typescript
interface Favorite {
  type: 'product' | 'article';

  // 产品收藏
  product?: {
    productId: string;
    productName: string;
    productImage: string;
    currentPrice: number;
    originalPrice?: number;
    stock: number;
    isValid: boolean;        // 是否已下架
  };

  // 文章收藏
  article?: {
    articleId: string;
    title: string;
    cover: string;
    category: string;
  };

  favoriteTime: Date;
}
```

#### 4.4 优惠券中心

**优惠券类型**：

```typescript
interface Coupon {
  couponId: string;
  name: string;
  type: 'discount' | 'cash' | 'shipping';

  // 折扣券
  discountRate?: number;     // 0.8 表示8折

  // 现金券
  discountAmount?: number;   // 减免金额
  conditionAmount?: number;  // 满X元可用

  // 免邮券
  freeShipping?: boolean;

  // 使用限制
  limitCategories?: string[]; // 限定品类
  limitProducts?: string[];   // 限定商品

  // 时间限制
  startTime: Date;
  endTime: Date;

  // 状态
  status: 'available' | 'used' | 'expired';
  usedTime?: Date;
  orderId?: string;
}
```

#### 4.5 积分体系

**积分规则**：

| 行为 | 获得积分 | 说明 |
|------|---------|------|
| **注册账号** | +100 | 仅首次 |
| **完善资料** | +50 | 填写生日、性别等 |
| **每日签到** | +5~20 | 连续签到翻倍 |
| **购物消费** | 1元=1积分 | 实付金额 |
| **评价晒单** | +10~50 | 带图+20，精华+50 |
| **分享商品** | +5 | 每天上限20 |
| **邀请好友** | +100 | 好友首单后获得 |

**积分用途**：

```typescript
interface PointsExchange {
  exchangeType: 'coupon' | 'product' | 'discount';

  items: Array<{
    id: string;
    name: string;
    image: string;
    pointsCost: number;      // 所需积分
    stock: number;           // 剩余库存
    monthLimit?: number;     // 每月兑换上限
  }>;
}
```

---

### 5. 美妆学院

#### 5.1 内容分类

**文章类型**：

```typescript
interface Article {
  articleId: string;
  title: string;
  subtitle: string;
  cover: string;
  category: 'skincare' | 'makeup' | 'tutorial' | 'ingredients';
  tags: string[];
  author: {
    name: string;
    avatar: string;
    title: string;          // 护肤专家、彩妆师
  };
  content: string;          // 富文本内容
  relatedProducts: string[]; // 关联商品ID
  publishTime: Date;
  viewCount: number;
  likeCount: number;
  commentCount: number;
}
```

#### 5.2 视频教程

**视频播放器功能**：

```typescript
interface VideoPlayer {
  videoUrl: string;
  poster: string;           // 封面图
  duration: number;         // 时长（秒）

  // 播放控制
  controls: {
    play: boolean;
    pause: boolean;
    speed: [0.5, 1.0, 1.25, 1.5, 2.0];
    volume: number;         // 0-1
    fullscreen: boolean;
    pip: boolean;           // 画中画
  };

  // 字幕
  subtitles: Array<{
    lang: string;
    src: string;
    default: boolean;
  }>;

  // 进度记录
  progress: {
    currentTime: number;
    watched: boolean;       // 是否看完
    lastPosition: number;   // 上次观看位置
  };
}
```

#### 5.3 互动功能

**评论系统**：

```typescript
interface Comment {
  commentId: string;
  userId: string;
  userInfo: {
    nickname: string;
    avatar: string;
    level: string;
  };
  content: string;
  images?: string[];
  replyTo?: string;         // 回复的评论ID
  replies: Comment[];       // 子评论
  likeCount: number;
  isLiked: boolean;
  createTime: Date;
}
```

---

### 6. 搜索功能

#### 6.1 搜索框设计

**搜索联想**：

```typescript
interface SearchSuggestion {
  // 历史搜索
  history: string[];

  // 热门搜索
  hotKeywords: Array<{
    keyword: string;
    rank: number;
    isHot: boolean;         // 是否标红
  }>;

  // 实时联想
  suggestions: Array<{
    keyword: string;
    type: 'product' | 'brand' | 'category';
    highlightText: string;  // 高亮匹配部分
  }>;
}
```

#### 6.2 搜索结果页

**综合搜索结果**：

```
搜索结果: "保湿精华"                      共找到123个结果
+--------------------------------------------------+
|  商品 (89)  |  文章 (24)  |  品牌 (10)           |
+--------------------------------------------------+
|  排序: [综合] [销量] [价格] [新品]               |
+--------------------------------------------------+
|  [商品1] [商品2] [商品3] [商品4]                 |
|  [商品5] [商品6] [商品7] [商品8]                 |
+--------------------------------------------------+
|  相关品牌推荐:                                    |
|  [品牌A] [品牌B] [品牌C]                         |
+--------------------------------------------------+
|  相关文章:                                        |
|  • 如何选择适合自己的保湿精华                     |
|  • 保湿精华的正确使用方法                         |
+--------------------------------------------------+
```

---

### 7. 客户服务

#### 7.1 在线客服

**客服系统功能**：

```typescript
interface ChatSystem {
  // 会话信息
  session: {
    sessionId: string;
    userId: string;
    agentId?: string;       // 客服ID
    status: 'waiting' | 'chatting' | 'closed';
    createTime: Date;
  };

  // 消息类型
  message: {
    messageId: string;
    type: 'text' | 'image' | 'product' | 'order' | 'quick-reply';
    content: string;
    sender: 'user' | 'agent' | 'system';
    timestamp: Date;
    isRead: boolean;
  };

  // 快捷回复
  quickReplies: Array<{
    id: string;
    text: string;
    category: string;
  }>;

  // 发送商品/订单卡片
  card: {
    type: 'product' | 'order';
    data: any;
  };
}
```

#### 7.2 帮助中心

**FAQ分类**：

```
帮助中心
├── 新手指南
│   ├── 如何注册账号
│   ├── 如何下单购买
│   ├── 如何使用优惠券
│   └── 如何查看订单
├── 支付相关
│   ├── 支持哪些支付方式
│   ├── 支付失败怎么办
│   └── 如何申请退款
├── 配送相关
│   ├── 配送范围和时效
│   ├── 运费如何计算
│   ├── 如何查看物流
│   └── 无法收货怎么办
├── 售后服务
│   ├── 退换货政策
│   ├── 如何申请售后
│   ├── 退款多久到账
│   └── 发票相关问题
└── 会员相关
    ├── 会员等级规则
    ├── 积分获取和使用
    ├── 优惠券领取规则
    └── 如何提升会员等级
```

---

*文档版本：v1.0*
*下一部分：后台管理系统功能设计*
