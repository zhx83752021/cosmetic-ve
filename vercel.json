{
  "buildCommand": "pnpm --filter @cosmetics/web run build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "npm install -g pnpm@8.12.1 && pnpm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
