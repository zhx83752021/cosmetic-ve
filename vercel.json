{
  "installCommand": "npm install -g pnpm@8.12.1 && NODE_ENV=development pnpm install --no-frozen-lockfile",
  "buildCommand": "cd apps/server && prisma generate && tsc && cd ../web && pnpm run build && cd ../.. && node scripts/prepare-deploy.js",
  "outputDirectory": "public",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
